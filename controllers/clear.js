// declare a variable db the points to the models folder
var db = require('../models')
// create a clear method that will remove a headline
	// and any associated notes
	// in the callback function we can return json (true or false if note was deleted)

// wrap this query in a module.exports wrapper