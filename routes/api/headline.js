// declare router variable and require ("express").Router()
var router = require('express').Router();
// declare headline controller and point to controllers/headline

// create a route (get) that points to headline controller variable and findAll() method
// create a router.delete and point to headline controller variable and delete() method
// create a router update (put) method that points to headlineController.update

//(we will use ids to query the db and update and delete headlines

// exports router