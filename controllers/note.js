// controller for our notes

// create variable db that points to models folder
var db = require('../models')

// (wrap in module.exports)

	// find one note
	
	// create a new note
	
	// delete a note with a given id